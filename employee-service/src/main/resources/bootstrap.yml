spring:
  profiles:
    active: instance2
  application:
    name: employee-service
  cloud:
    config:
      uri: http://localhost:8088
feign:
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream